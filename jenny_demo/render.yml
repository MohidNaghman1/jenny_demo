services:
  - type: web
    name: jenny-demo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PORT
        value: 8000
    autoDeploy: true